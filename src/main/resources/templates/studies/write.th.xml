<?xml version="1.0"?>
<thlogic>
    <attr sel="#study-form" th:action="'/studies/posts'" th:method="post">
        <attr sel="#title" th:value="${study?.title} ?: _" />
        <attr sel="#editor" th:text="${study?.body} ?: _" />
        <attr sel="#cancel-button" th:onclick="'history.back()'" />
    </attr>
</thlogic>
